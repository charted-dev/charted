package org.noelware.charted.gradle.plugins.homebrew;

import org.gradle.api.Plugin;
import org.gradle.api.Project;

public class HomebrewPlugin implements Plugin<Project> {
    @Override
    public void apply(Project project) {}
}
